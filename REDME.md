# Notas:
primer programa en Node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     base para la tabla                          [number] [required]
  -l, --listar   muestra o no la tabla                [boolean] [default: false]
  -f, --final    numero hasta el cual llega la tabla      [number] [default: 10]
  ```